<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:visibility="invisible">
    <data>
        <variable
            name="fragment"
            type="com.example.fiiirst.fragments.MainFragment" />
    </data>
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp"
        app:cardUseCompatPadding="true"
        app:cardBackgroundColor='@{fragment.valid ? @color/green : @color/red}'
        android:layout_marginTop="10dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:background='@{fragment.valid ? @color/green : @color/red}'>
            <TextView
                android:id="@+id/main_fragment_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text='@{fragment.title}'>
            </TextView>
            <TextView
                android:id="@+id/main_fragment_description"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text='@{fragment.description}'>
            </TextView>
        </LinearLayout>
    </androidx.cardview.widget.CardView>

</layout>